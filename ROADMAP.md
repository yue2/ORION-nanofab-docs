## Content

- [ ] revise docs in user-guide/
  - [ ] imaging.md need to check flood gun default settings
- [ ] add NPVE User Guide PDF to `sources/`; link it from `docs/reference/original-documents.md`; create its `docs/information-base/` transcription page; cite it on `docs/user-guide/patterning.md` (advanced pattern/dose steps) and on the reference page

- [ ] audit docs/superuser/training/ for completeness and source traceability
- [ ] audit docs/superuser/maintenance/ for procedure gaps and safety conflicts
- [ ] audit docs/superuser/error-recovery/ for conflicting guidance vs. ZEISS manual
- [ ] audit docs/safety/ for emergency procedures and high-voltage warnings
- [ ] audit docs/reference/ for glossary completeness and broken internal links
- [ ] audit docs/information-base/ for source PDF transcription completeness
- [ ] verify all `sources:` frontmatter fields reference existing PDFs under `sources/`
- [ ] verify all image paths in Markdown point to valid files under `assets/img/`
- [ ] verify access-level tags (`all-users`, `superuser`) match folder structure
- [ ] resolve all **WARNING — CONFLICTING GUIDANCE** blocks with Tool Owner approval
- [ ] resolve all **WARNING — WAITING FOR MANUAL CONFIRMATION** blocks
- [ ] update `last-reviewed` dates for all published pages
- [ ] check for orphaned or unreferenced pages in NAV

## Code

Implemented (verify in the browser before closing):
- [x] SPA `data-page` routing and page rendering in `app.js` (loader + `_pages/*.json`)
- [x] pagination `<nav>` injected by `build_site.py` from `nav_previous_path` / `nav_next_path` frontmatter
- [x] full-text search index with per-page access metadata (`assets/search-index.json`)
- [x] access-level filtering of search results in `app.js` (user vs. superuser role)
- [x] `nav_order.json` regenerated by `build_site.py` from the `NAV` list (single source of order)
- [x] folder exports via `export_guide.py` flags derived from `nav_order.json`
- [x] build and export workflow documented in `README.md` and `MAINTAINING_THE_DOCS.md`

Remaining code/content work:
- [ ] fix malformed pagination frontmatter in `docs/user-guide/`:
      duplicate `nav_previous:` keys / `nav_previous: null` in `before-you-start.md`,
      trailing space after `nav_previous_path:` in `loading-unloading.md`
- [ ] add `nav_previous_path` / `nav_next_path` to the remaining user-guide pages
      (`imaging`, `patterning`, `neon`, `ending-session`, `warning-signals`)
- [ ] manual QA: pagination navigation across all user-guide pages
- [ ] manual QA: pagination navigation across all superuser pages
- [ ] manual QA: search access filtering (user vs. superuser role)
- [ ] manual QA: verify export page order matches site navigation after a `NAV` change
- [ ] rename `export_guide.py` → `build_guide.py` to match README/MAINTAINING docs
      (or update the docs to `export_guide.py`)
- [ ] add automated smoke tests for `build_site.py` and `build_guide.py` (none exist yet)
- [ ] add CI/CD workflow for automated build + export (`.github/workflows/`)
- [ ] create CONTRIBUTING.md for colleague onboarding
- [ ] upload to GitHub (`git remote -v`) and configure repo settings

## Review & Handoff

- [ ] Request Tool Owner sign-off on all flagged procedural conflicts
- [ ] Brief team on documentation structure and audit rules (AGENTS.md)
- [ ] Set up GitHub branch protection for documentation PRs
- [ ] Archive source PDFs in `sources/` as immutable reference

## Framework Packaging (future)

Generalize the documentation-control framework implemented here so it can be reused for other tools/instruments:

- [ ] document the framework pillars in a design note: Markdown-first content under `docs/`, YAML frontmatter metadata, single source of order (`NAV` in `build_site.py` → `nav_order.json`), access-level model (`all-users` / `superuser`)
- [ ] refactor `build_site.py` and `export_guide.py` (folder-export flags, output naming, `--out`) into a reusable, config-driven package with per-tool configuration instead of hardcoded paths/slugs
- [ ] package the read-only documentation reviewer rules (`AGENTS.md` + `.agent.md`) as a reusable agent template with tool-specific content rules
- [ ] make the export flag list and default output names derive automatically from the target tool's `nav_order.json` (no per-tool flag code)
- [ ] add a smoke-test suite for the packaged build/export scripts so new tools inherit tested behavior
- [ ] write a short "adopting the framework for a new tool" runbook (directory scaffold, first NAV, frontmatter conventions, audit checklist)
- [ ] decide artifact/licensing home for the packaged framework (separate repo vs. template branch) and document the import path for future tools
